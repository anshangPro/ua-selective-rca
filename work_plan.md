# 两个月工作计划：检测边界不确定性感知的选择性微服务 RCA

## 总体目标

构建并验证“检测边界不确定性的后验加权 + 自适应窗口聚合 + 选择性 Top-k 根因定位”方法，形成可投 EI 的完整包：开源可复现实验代码、数据处理脚本、完整实验结果、论文初稿与附录材料。

核心方法不改变 CIRCA、RCD、BARO 的根因排序内核；它解决其共同的隐含假设——故障检测时刻精确可靠。方法输出不再强制给出单一根因，而是依据置信度返回 Top-1、最小 Top-k 集合或拒判。

## 方法与创新点

- **后验检测边界建模**：以至少两类检测器产生候选故障起点及分数，经温度校准后形成 \(p(t_s\mid X,D)\)，而非固定单一检测时刻。
- **自适应窗口聚合**：围绕多个候选起点构造分析窗口；分别调用 CIRCA、RCD、BARO 获取候选根因排序，并以边界后验、窗口质量和跨窗口排序稳定性加权融合。
- **选择性诊断机制**：基于排序间隔、边界熵、跨窗口稳定性和预测置信度，自动选择 Top-1、最小 Top-k 或拒判。
- **完整鲁棒评测协议**：以检测起点偏移、不同检测器、不同系统和不同故障类型验证性能；同时报告准确性、覆盖率、风险、校准度与计算开销。
- **研究边界**：不采用“时序异常聚类 + RAG/LLM 归因”路线，明确区别于 TRAC-RCA 类工作；贡献聚焦于“不确定检测边界如何传递并约束 RCA 决策”。

## 实施安排

| 周次    | 任务                                                         | 验收目标                                               |
| ------- | ------------------------------------------------------------ | ------------------------------------------------------ |
| 第 1 周 | 环境、RCAEval RE1 数据与案例审计；统一指标、数据切分和运行记录；跑通 CIRCA、RCD、BARO | 三个基线在同一数据接口上可运行；形成数据字典与案例清单 |
| 第 2 周 | 建立故障起点偏移实验；复现 Oracle 时间点、单检测时间点、等权多窗口结果 | 获得偏移曲线，确认检测时刻误差对 RCA 的实际影响        |
| 第 3 周 | 实现至少两类检测器与候选边界后验：变点检测器、统计/预测残差检测器；完成温度校准 | 每个案例输出候选起点、概率分布、熵和校准指标           |
| 第 4 周 | 实现后验加权窗口聚合、窗口质量评分与稳定性因子；分别接入 CIRCA、RCD、BARO | 完成关键消融结果                                       |
| 第 5 周 | 实现选择性 Top-k 与拒判策略；确定阈值仅在验证集调参          | 输出 Top-1、Top-k、拒判和置信解释；生成风险—覆盖率曲线 |
| 第 6 周 | 在 RE1 三个系统完成主实验与显著性检验                        | 完整主表、偏移鲁棒曲线、消融表和运行成本表             |
| 第 7 周 | 在 RE2 指标子集或 BARO 公开数据上做外部验证；完成敏感性分析  | 验证不同检测器、窗口宽度、候选数和温度参数下的稳定性   |
| 第 8 周 | 固化随机种子、配置、日志和一键脚本；撰写论文初稿、附录与开源说明 | 可复现实验包、论文初稿、图表与结果归档完成             |

## 实验设计与验收

- 数据：以 RCAEval RE1 为主，RE2 指标子集或 BARO 公开数据为外部验证；训练、调参与测试严格按故障案例划分，禁止同一案例窗口泄漏。
- 对照：Oracle 故障时刻、单检测时刻、等权多窗口、后验聚合、后验聚合+稳定性、完整选择性方法；排序器使用 CIRCA、RCD、BARO。
- 偏移：以数据采样间隔折算测试 \(\{-300,-120,-60,-30,0,30,60,120,300\}\) 秒或等价窗口偏移。
- 指标：AC@1、AC@3、AC@5、MRR、Avg@5；偏移性能曲线、面积、最差性能和方差；Coverage、Selective Risk、AURC、Top-k 集合召回率、平均集合大小、拒判率与校准误差。
- 成功标准：
  - 相比“同一排序器 + 单检测时刻”，至少两个系统的 AC@1 或 MRR 有稳定提升；
  - 相比等权窗口，后验或稳定性模块在至少一项核心指标上有独立增益；
  - 在 70%、80%、90% 覆盖率下，选择性输出的风险低于强制 Top-1；
  - 总推理成本不超过单窗口方案的 3–5 倍；
  - 报告全部负结果，避免仅保留有利案例。

## 工程交付与风险控制

- 采用统一输入接口：`metrics / logs / traces / candidate_boundaries / posterior`；统一输出接口：`ranked_services / confidence / decision_mode / top_k / abstention_reason`。
- GPU 可作为检测器训练、深度表征和多窗口并行推理的正式计算资源；实验同时记录 GPU 型号、显存、CUDA/驱动、运行时间和显存占用。
- 核心实验应提供 GPU 运行配置，并保留轻量 CPU 复现配置，使结论不依赖某一特定硬件。
- 每次实验保存数据版本、配置、随机种子、检测器分数、候选边界后验、窗口列表、最终决策和运行时间。
- RCD 如全量成本过高，可优先采用 GPU 并行、缓存中间结果；仍超预算时保留固定代表性子集及原因说明，不牺牲 CIRCA、BARO 的全量主实验。
- 若后验聚合不优于等权窗口，转而分析其适用条件，并将贡献收敛为“在高边界熵场景中降低选择性风险”的条件性结论。
- 最终开源包应包含环境文件、下载与预处理说明、案例划分、全部配置、复现实验脚本、结果汇总脚本及论文图表生成脚本。

## 默认假设

- 不限制 GPU 使用；模型与检测器可采用 GPU 加速，优先利用多窗口并行提升实验吞吐。
- 选择性诊断保留拒判能力，不为提高覆盖率强制输出单一根因。
- CIRCA、RCD、BARO 均采用其公开实现与公开数据接口；若某实现不可稳定复现，须保留失败记录并以可运行基线替代，同时不改变方法主体。