# 面向检测边界不确定性的选择性微服务根因定位



“检测边界”：故障究竟从哪个时间点开始，或异常窗口的起止位置。

“不确定性”：检测器通常只能说“可能在这一段附近出了问题”，不能精确保证某一秒就是故障起点。

“后验加权”：不把某一个检测时刻当真，而是给多个可能起点分配可信度，再综合各起点下的 RCA 结果。

“选择性根因定位”：证据足够时输出单一根因；证据不够时输出 Top-k 候选及不确定性；再不够时拒绝做出高置信结论。



解决的问题是：很多 RCA 方法默认已经知道真实故障发生时刻 \(t_f\)。但线上真实情况是：此时间点未知或存在偏移



## 1. 项目定位

**暂定题目（中文）**：面向检测边界不确定性的后验加权与选择性根因定位方法

**暂定题目（英文）**：*Posterior-Weighted Selective Root Cause Analysis under Detection-Boundary Uncertainty for Microservices*

本项目研究微服务故障诊断中的一个常被忽略的链路：异常检测（AD）给出的故障开始时刻通常有延迟、提前或多解，而很多根因分析（RCA）方法把这个时刻当作精确输入，以此切分正常与异常数据，再输出唯一根因。目标不是单独提高异常检测准确率，也不是把多窗口结果简单投票；目标是让 RCA **显式接收并传播检测边界的不确定性**，在证据不足时避免过度自信地输出单一根因。

建议以 RCAEval 的指标数据为主实验平台，以 BARO、CIRCA、RCD、NSigma 为主要基线。RCAEval 提供公开实现、故障注入时间和带根因标注的多系统数据；RE1 为 375 个仅指标案例，RE2/RE3 可留作多模态扩展验证。[RCAEval](https://github.com/phamquiluan/RCAEval) [数据说明](https://zenodo.org/records/14590730)

## 2. 问题背景与研究缺口

设微服务系统在离散时刻 $t=1,\ldots,T$ 产生 $M$ 条监控序列：

$$X=\{x_{m,t}\mid m\in\{1,\ldots,M\},t\in\{1,\ldots,T\}\}.$$ 

一个故障在真实开始时刻 $t_f$ 发生，根因集合为 $C^*$（服务级或指标级）。实际部署时，异常检测器不提供 $t_f$，而给出警报时刻、变点分数或候选起点 $\hat t_f$。通常 $\hat t_f\neq t_f$。

既有 RCA 往往以单点 $\hat t_f$ 切分正常/异常区间：

$$X^{normal}=X_{t<t_f}, \quad X^{abnormal}=X_{t\geq t_f}.$$ 

一旦切分边界偏移，正常集会混入异常、异常集会混入传播后的次生症状，进而改变因果图、假设检验或候选排序。ASE 2024 的系统评测已指出 CIRCA、RCD、NSigma 对估计故障时刻敏感，并明确建议评估不同异常检测器和端到端 AD-RCA 流程。[Pham et al., ASE 2024](https://arxiv.org/pdf/2408.13729)

BARO 是最接近的端到端基线：它以多变量在线贝叶斯变点检测触发 RCA，并将**第一个检测到的异常**近似为故障发生时刻。[BARO 论文](https://openreview.net/pdf?id=AjUG67YOY4) [BARO 代码](https://github.com/phamquiluan/baro) 该单点近似是本项目要放松的核心假设。

## 3. 正式问题定义

### 3.1 输入

- 多变量遥测序列 $X$，至少包含指标；可选地包含日志和 trace。
- 一个或多个异常检测器 $D_j$ 输出的时间分数 $a_j(t)$、候选变点或警报。
- RCA 基方法 $g$，给定诊断窗口 $w$ 后输出根因候选 $c$ 的排序分数 $r_g(c\mid w)$。
- 可用于离线评估的根因标注 $C^*$ 与故障注入时刻 $t_f$；线上运行不依赖它们。

### 3.2 检测边界后验

将检测器输出转化为开始时刻的离散后验分布：

$$p(t_s\mid X,D)=\frac{\exp(\alpha\,s(t_s))}{\sum_{u\in\mathcal T}\exp(\alpha\,s(u))},$$

其中 $s(t_s)$ 可由单检测器变点分数、多个检测器校准后的加权分数，或候选起点的经验似然得到；$\mathcal T$ 是警报附近的有限候选时间集合。该分布不是声称恢复真实贝叶斯后验，而是一个经校准的**边界可信度分布**，需通过保留验证集或无监督稳定性策略确定温度 $\alpha$。

对每个候选起点构造一个或多个诊断窗口：

$$w=(t_s-L_{pre},\;t_s+L_{post}), \quad w\in\mathcal W.$$ 

### 3.3 不确定性感知根因聚合

每个窗口运行同一 RCA 基方法，得到 $r_g(c\mid w)$。对候选根因定义：

$$R(c)=\sum_{w\in\mathcal W}p(w\mid X,D)\cdot E(c,w)\cdot r_g(c\mid w),$$

其中：

- $p(w\mid X,D)$：由检测边界后验诱导的窗口权重；
- $E(c,w)$：窗口级证据质量，例如异常强度、正常/异常样本量充足性、因果图可用性；
- $r_g(c\mid w)$：经归一化的基 RCA 分数。

为避免“高分但对边界极端敏感”的候选，定义窗口稳定性：

$$S(c)=1-\operatorname{Var}_{w\sim p(w\mid X,D)}[\widetilde r_g(c\mid w)],$$

并使用 $R'(c)=R(c)\cdot S(c)$ 排名。实际实现中可改用 Spearman 排名相关性、Top-$k$ 出现频率或熵，避免不同 RCA 分数尺度不可比。

### 3.4 选择性诊断输出

系统输出不是固定的 Top-1，而是三种有条件的诊断动作：

1. **单一根因**：若最高候选同时满足高聚合置信度、足够的首二名间隔及跨窗口稳定性，则输出 $c_1$。
2. **集合诊断**：若单一候选证据不足，但存在一组候选覆盖足够后验质量，则输出最小 Top-$k$ 集合 $\mathcal C_k$、每个候选分数及边界不确定性。
3. **拒判/升级**：若无候选集合达到最小可靠性条件，则拒绝给出确定诊断，并请求延长观测、补充 trace/log、或等待下一窗口。

可定义单一输出置信度：

$$Conf=\lambda_1\,Margin+\lambda_2\,S(c_1)+\lambda_3\,(1-H(p(t_s\mid X,D))/\log |\mathcal T|)+\lambda_4\,Q,$$

其中 $Margin$ 为 $c_1$ 与 $c_2$ 的聚合分差，$H$ 是检测边界熵，$Q$ 是窗口质量。若 $Conf\geq \tau_{single}$，输出单一根因；否则选择满足累计候选质量阈值 $\eta$ 的最小集合；若集合质量仍低于 $\tau_{set}$，则拒判。

**关键约束**：阈值必须在验证集或无标签风险控制程序中确定，不能使用测试集标签调参。

## 4. 基于什么工作，做出什么改进

| 基础工作 | 已有能力/假设 | 本项目改进 |
|---|---|---|
| BARO（FSE 2024） | AD 与 RCA 端到端衔接；首个检测异常近似故障开始时刻 | 单点开始时刻改为候选边界分布；跨窗口聚合与稳定性惩罚 |
| CIRCA / RCD / NSigma | 以给定故障时刻或正常/异常切分执行因果/统计 RCA | 将其作为可插拔 $g$，检验方法对边界误差的鲁棒性是否可被统一提升 |
| RCAEval / ASE 2024 评测 | 揭示故障时刻误设会改变 RCA 性能；提供可复现实验框架 | 建立 AD 边界误差—RCA 风险—选择性输出的端到端评测协议 |

## 5. 当前任务的创新点

### I1. 检测边界不确定性到 RCA 排名的显式传播

将异常检测输出由“唯一故障开始时刻”提升为候选边界的可信度分布，并将其作为 RCA 排名聚合的显式权重。创新不在使用多个窗口，而在于将边界不确定性、窗口证据和 RCA 结果统一进入可解释的决策函数。

### I2. 面向边界扰动的稳定性正则/排序聚合

对同一事件在相邻候选窗口中的 RCA 输出建模，惩罚依赖某个偶然切分点才成立的根因。该机制将“排名稳定性”作为诊断可靠性的一部分，而非仅报告平均 Top-$k$。

### I3. 选择性、集合值的微服务 RCA

传统 RCA 必须输出 Top-1；本项目允许系统在证据充分时给单一根因，在边界不确定时返回带置信度的最小 Top-$k$，在信息不足时拒判。研究问题从“永远猜一个根因”转变为“在给定覆盖率下最小化诊断风险”。

### I4. 检测误差下的端到端鲁棒评测协议

以真实检测器输出和受控边界扰动替代 oracle 故障时刻，报告性能退化曲线、选择性风险—覆盖率曲线和集合诊断效用。这是对当前常见“给定精确注入时刻”评测方式的补充。

## 6. 与相邻工作的边界

2026 年预印本 TRAC-RCA 在粗粒度故障区间内对异常进行**时间域 DBSCAN 聚类**，然后逐簇进行 RAG/LLM RCA 并比较多候选假设。[TRAC-RCA](https://github.com/grampus-whcz/TRAC-RCA) 本项目不采用时间聚类、RAG、LLM 推理或“每簇独立诊断”的路线。

本项目的技术边界应始终表述为：

> 研究异常检测器对故障开始边界的**不确定性**如何传播到以因果/统计排序为核心的 RCA，并在该不确定性下进行风险受控的单点、集合或拒判输出。

这一区别必须在引言、方法图、消融实验与 related work 中反复说明。不要使用“Temporal Anomaly Clustering”“基于时间簇的 RCA”等标题或关键词。

## 7. 研究问题与可检验假设

| 编号 | 研究问题 | 可检验假设 |
|---|---|---|
| RQ1 | 故障起点偏移如何影响现有 RCA？ | CIRCA/RCD/NSigma 在较大偏移下显著退化；BARO 相对更稳但并非不受影响 |
| RQ2 | 后验加权聚合是否提高边界误差下的诊断质量？ | 在相同 RCA 基方法下，本方法的 worst-case Top-$k$、MRR 或 Avg@5 优于单点边界与等权窗口集成 |
| RQ3 | 选择性输出是否能在不可靠事件上降低误诊风险？ | 随拒判比例增加，条件风险单调下降；同等覆盖率下，本方法优于最大分数阈值基线 |
| RQ4 | 改进是否跨检测器、系统规模与故障类型稳定？ | 在至少两种检测器、三个系统和多个故障类型上保持收益或非劣表现 |

## 8. 实验设计与成功标准

### 数据与划分

- **主数据**：RCAEval RE1（375 个指标案例，三套系统）。
- **主要验证**：RCAEval RE2 的指标子集；条件允许时将日志/trace 用于“补充遥测后能否降低拒判率”的扩展实验。
- **辅助复现**：BARO 的公开三系统数据及原脚本。
- 按故障案例而非时间点划分 train/validation/test；同一注入实例的窗口不得跨集合泄漏。

### 对比方法

1. Oracle 边界 + 原 RCA：性能上界，不作为真实部署基线。
2. 单点检测边界 + 原 RCA：现实主基线。
3. 单点边界 + BARO：端到端强基线。
4. 等权多窗口 + 原 RCA：验证“并非简单集成带来全部收益”。
5. 本方法：后验加权 + 稳定性聚合 + 选择性输出。

### 边界误差条件

- 人工偏移：$\Delta\in\{-300,-120,-60,-30,0,30,60,120,300\}$ 秒，按数据采样频率调整。
- 真实检测：至少一个变点检测器和一个统计/预测型检测器；记录实际警报延迟。
- 多重/噪声告警：在候选窗口加入无关异常或相邻事件，测试拒判是否生效。

### 指标

- 常规 RCA：AC@1、AC@3、AC@5、Avg@5、MRR。
- 鲁棒性：各偏移下性能、最坏偏移性能、面积指标（performance-vs-offset AUC）、方差。
- 选择性：coverage、selective risk、risk-coverage curve、AURC、单一输出准确率、集合 Recall@k、平均集合大小、拒判率。
- 校准：边界分布熵与实际边界误差/诊断正确率的相关性；必要时 ECE 或 Brier score。
- 成本：单案例运行时间、窗口数、相对原 RCA 的倍数。

### 最低成功门槛

- 在至少两个真实微服务系统上，非 oracle 条件下优于“单点检测边界 + 同一 RCA”。
- 相比等权多窗口，显示后验/稳定性至少一个组件的独立增益。
- 在指定覆盖率（例如 70%、80%、90%）下，选择性风险低于强制 Top-1 输出。
- 平均运行成本保持在基础 RCA 的可接受倍数内；需预先报告预算，如不超过 3–5 倍。
- 对负面结果如某些故障类型无收益，必须如实保留并解释。

## 9. 八周工作计划

| 周次 | 工作包 | 具体产物 / 验收目标 |
|---|---|---|
| 第 1 周 | 环境与数据审计 | 复现 RCAEval 基础测试；下载 RE1；建立按 case 划分的数据清单；记录数据版本、依赖与硬件 |
| 第 2 周 | 基线复现与误差诊断 | 跑通 BARO、CIRCA 或 RCD 中至少两个；实现单点边界偏移脚本；得到第一版性能—偏移曲线 |
| 第 3 周 | 检测边界建模 | 接入两个检测器或一个检测器的可校准多阈值输出；实现候选边界集与 $p(t_s\mid X,D)$；完成无标签/验证集校准方案 |
| 第 4 周 | 核心聚合算法 | 实现后验加权、证据质量、稳定性项；完成单点、等权窗口、本方法三组消融 |
| 第 5 周 | 选择性诊断 | 实现单一/Top-$k$/拒判决策；实现风险—覆盖率、集合大小与边界不确定性可视化 |
| 第 6 周 | 大规模实验 | 在 RE1 三系统、多个偏移和故障类型上运行；保存所有原始排名、配置、随机种子 |
| 第 7 周 | 扩展与稳健性 | 在 RE2 指标子集或 BARO 数据集验证；测试检测器替换、窗口长度和超参数敏感性 |
| 第 8 周 | 论文材料与复现包 | 固化代码、配置、运行脚本和结果表；完成方法图、主表、消融图、威胁与相关工作草稿 |

## 10. 任务目标清单

- [x] 固定一个公开数据版本、代码 commit 和 Python 环境，形成可重复环境说明。
- [x] 复现至少两个 RCA 基线在一个公开数据集上的结果或给出偏差解释。
- [ ] 构建检测边界候选集与概率/置信度表示，而非仅使用单一警报时刻。
- [ ] 实现后验加权 + 窗口稳定性聚合，并提供独立消融。
- [ ] 实现单根因、Top-$k$ 集合和拒判三种输出模式。
- [ ] 建立不使用 oracle 时刻的端到端评测流程。
- [ ] 完成不同偏移、不同检测器、不同系统的鲁棒性实验。
- [ ] 报告风险—覆盖率与集合诊断效用，而非只报告 Top-$k$。
- [ ] 对比单点检测、等权窗口、BARO 和至少一种因果 RCA。
- [ ] 输出可复现实验包：数据获取说明、依赖锁定、配置、种子、命令和原始结果。
- [ ] 在论文中明确与 TRAC-RCA 的任务、机制和评价对象差异。

## 11. 风险与降级方案

| 风险 | 影响 | 缓解/降级 |
|---|---|---|
| RCD/CIRCA 运行慢 | 多窗口导致成本放大 | 先在 RE1-OB/SS 开发；限制候选窗口数；将 BARO/NSigma 作为快速基线；报告效率折中 |
| 检测器无法输出严格概率 | 后验解释不严谨 | 使用“校准的边界可信度”措辞；采用 softmax 温度、保序校准或 detector ensemble；不宣称严格贝叶斯保证 |
| 聚合没有超过强基线 | 方法贡献不充分 | 分析适用条件；引入选择性风险目标或证据质量项；保留负面结果，不以测试集调参 |
| TRAC-RCA 相邻度高 | 审稿人认为时间范围处理重复 | 不用聚类/RAG/LLM；强调检测后验、因果排序、风险受控输出；加入直接差异表与等权窗口消融 |
| 多模态扩展超时 | 影响两月交付 | 以 metrics-only 闭环作为主线；RE2 多模态仅作为“拒判后补充遥测”的可选附录 |

## 12. 预期论文贡献表述

> 本文提出一种面向微服务根因定位的选择性端到端诊断框架。该框架不再将异常检测结果视为精确故障开始时刻，而是将检测边界的不确定性表示为候选窗口上的可信度分布，并通过后验加权与跨窗口稳定性聚合生成鲁棒的根因排序。进一步地，框架依据诊断置信度在单一根因、最小候选集合与拒判之间选择输出，从而在不可靠检测条件下控制错误诊断风险。基于多个公开微服务基准的实验表明，该框架能够降低 RCA 对故障时刻偏移的敏感性，并提供传统强制 Top-1 RCA 缺少的风险—覆盖率权衡。

## 13. 参考文献与资源

1. Pham, Ha, Zhang. *Root Cause Analysis for Microservices based on Causal Inference: How Far Are We?* ASE 2024. [PDF](https://arxiv.org/pdf/2408.13729)
2. Pham, Ha, Zhang. *BARO: Robust Root Cause Analysis for Microservices via Multivariate Bayesian Online Change Point Detection.* FSE 2024. [代码](https://github.com/phamquiluan/baro)
3. Pham et al. *RCAEval: A Benchmark for Root Cause Analysis of Microservice Systems with Telemetry Data.* WWW 2025. [代码](https://github.com/phamquiluan/RCAEval) [数据](https://zenodo.org/records/14590730)
4. Wang et al. *TRAC-RCA: A Tool-Augmented Framework for Microservice Root Cause Analysis via Temporal Anomaly Clustering and RAG.* 2026 preprint. [仓库](https://github.com/grampus-whcz/TRAC-RCA)
5. Mishra et al. *Conditional Attribution for Root Cause Analysis in Time-Series Anomaly Detection.* 2026 preprint. [arXiv](https://arxiv.org/abs/2604.17616)
